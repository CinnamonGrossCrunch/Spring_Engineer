# The companion CAD service deploys as its OWN Vercel project, using
# cad-service/ as that project's Root Directory and Dockerfile.vercel as its
# container image. Excluding it here keeps it out of the Next.js project's
# upload; the two are wired together at runtime through CAD_SERVICE_URL.
cad-service/
